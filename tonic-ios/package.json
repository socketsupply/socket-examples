{
  "name": "op-ios-example",
  "version": "1.0.0",
  "main": "main.js",
  "devDependencies": {
    "clean-css": "^5.1.1",
    "esbuild": "^0.14.38",
    "stylus": "^0.54.8"
  },
  "scripts": {
    "start": "op . -ios -simulator -r",
    "build": "op . -c -p",
    "watch": "op . -r --port=8000 & ./build.js --watch=`op . --target`"
  },
  "dependencies": {
    "@socketsupply/tonic": "^14.1.5"
  }
}
